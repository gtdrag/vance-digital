---
phase: 01-state-foundation-cli-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - .gitignore
  - src/cli/index.ts
  - src/db/client.ts
  - src/db/schema.ts
  - src/db/migrations/20260303000001_initial_schema.ts
  - src/config/loader.ts
  - src/config/schema.ts
  - src/config/paths.ts
  - src/cli/ui/logger.ts
  - src/cli/ui/colors.ts
  - src/types/index.ts
  - .env.example
autonomous: true
requirements:
  - CLI-06
  - CLI-07
  - BUDG-01

must_haves:
  truths:
    - "TypeScript project compiles with zero errors"
    - "SQLite database can be created with full schema (config, transactions, memes, domains, deployments, sites, nostr_keypairs tables)"
    - "Kysely migrations run successfully and are versioned"
    - "Config loads from XDG-compliant paths with environment variable precedence over config file values"
    - "Logger outputs with timestamps, severity levels, and respects quiet/pipe detection"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all Phase 1 dependencies"
      contains: "domainweave"
    - path: "tsconfig.json"
      provides: "TypeScript configuration for ESM project"
      contains: "module"
    - path: "src/db/client.ts"
      provides: "Database connection singleton with WAL mode"
      exports: ["getDb", "initializeDb"]
    - path: "src/db/schema.ts"
      provides: "Kysely type definitions for all tables"
      exports: ["DatabaseSchema"]
    - path: "src/db/migrations/20260303000001_initial_schema.ts"
      provides: "Full database schema migration"
      exports: ["up", "down"]
    - path: "src/config/loader.ts"
      provides: "Config loading with env var precedence"
      exports: ["loadConfig"]
    - path: "src/config/schema.ts"
      provides: "Zod validation schema for config"
      exports: ["ConfigSchema", "Config"]
    - path: "src/config/paths.ts"
      provides: "XDG directory resolution with fallbacks"
      exports: ["getConfigDir", "getDataDir"]
    - path: "src/cli/ui/logger.ts"
      provides: "Structured logger with severity levels"
      exports: ["createLogger", "Logger"]
  key_links:
    - from: "src/db/client.ts"
      to: "src/db/schema.ts"
      via: "Kysely<DatabaseSchema> type parameter"
      pattern: "Kysely<DatabaseSchema>"
    - from: "src/db/client.ts"
      to: "src/config/paths.ts"
      via: "getDataDir() for database file path"
      pattern: "getDataDir"
    - from: "src/config/loader.ts"
      to: "src/config/schema.ts"
      via: "Zod parse for validation"
      pattern: "ConfigSchema\\.parse"
    - from: "src/config/loader.ts"
      to: "src/config/paths.ts"
      via: "getConfigDir() for config file path"
      pattern: "getConfigDir"
---

<objective>
Set up the TypeScript project scaffold, SQLite database with full schema via Kysely migrations, config system with XDG-compliant paths and Zod validation, and structured logger.

Purpose: Every other plan in Phase 1 depends on these foundations. Database schema, config loading, and logging must exist before CLI commands or budget logic can be built.

Output: Compilable TypeScript ESM project with database, config, and logging modules ready for consumption by Plans 02 and 03.
</objective>

<execution_context>
@/Users/georgedrag/.claude/get-shit-done/workflows/execute-plan.md
@/Users/georgedrag/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-state-foundation-cli-scaffold/01-CONTEXT.md
@.planning/phases/01-state-foundation-cli-scaffold/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize TypeScript ESM project with all Phase 1 dependencies</name>
  <files>package.json, tsconfig.json, .gitignore, .env.example, src/types/index.ts</files>
  <action>
Create the Node.js TypeScript ESM project from scratch:

**package.json:**
- `"name": "domainweave"`, `"version": "0.1.0"`, `"type": "module"`
- `"bin": { "domainweave": "./dist/cli/index.js", "dw": "./dist/cli/index.js" }`
- `"engines": { "node": ">=22.0.0" }`
- `"scripts": { "build": "tsc", "dev": "tsx src/cli/index.ts", "test": "vitest run", "test:watch": "vitest" }`
- Dependencies: `commander`, `better-sqlite3`, `kysely`, `zod`, `picocolors`, `ora`, `cli-table3`, `boxen`, `xdg-basedir`
- DevDependencies: `typescript`, `@types/node`, `@types/better-sqlite3`, `tsx`, `vitest`
- Use `npm` as package manager (Claude's discretion: npm is simpler for initial setup, pnpm can be adopted later)

**tsconfig.json:**
- Target: `ES2022`, Module: `NodeNext`, ModuleResolution: `NodeNext`
- `"outDir": "./dist"`, `"rootDir": "./src"`
- Enable strict mode, sourceMap, declaration
- Include `src/**/*`

**.gitignore:** Append Node.js entries to existing file:
- `node_modules/`, `dist/`, `.env`, `*.db`, `*.db-wal`, `*.db-shm`
- Keep existing `.vercel` entry

**.env.example:** Template with all Domainweave env var names:
- `DOMAINWEAVE_REDDIT_KEY=`, `DOMAINWEAVE_ANTHROPIC_KEY=`, `DOMAINWEAVE_CLOUDFLARE_KEY=`, `DOMAINWEAVE_GITHUB_KEY=`, `DOMAINWEAVE_VERCEL_KEY=`

**src/types/index.ts:** Shared types stub exporting `TransactionCategory` type:
```typescript
export type TransactionCategory = 'domain_registration' | 'domain_renewal' | 'api_anthropic' | 'api_reddit' | 'api_google' | 'api_vercel' | 'other';
```

Create directory structure: `src/cli/commands/`, `src/cli/ui/`, `src/db/migrations/`, `src/db/queries/`, `src/config/`, `src/budget/`, `src/types/`

Run `npm install` after creating package.json.
  </action>
  <verify>
    <automated>cd /Users/georgedrag/APP_PROJECTS/vance.digital && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>package.json exists with correct bin field, tsconfig.json compiles, all dependencies installed, directory structure created, .gitignore updated with Node.js entries</done>
</task>

<task type="auto">
  <name>Task 2: Create SQLite database client and full schema migration</name>
  <files>src/db/client.ts, src/db/schema.ts, src/db/migrations/20260303000001_initial_schema.ts</files>
  <action>
**src/db/schema.ts** — Kysely type definitions for ALL tables (full schema upfront per user decision):

Tables to define (use `Generated<T>` for auto-increment/default columns):
- `config`: key (text PK), value (text), updated_at
- `transactions`: id (integer PK auto), category (text — use TransactionCategory type), amount_usd (real), description (text nullable), reference_id (text nullable — links to domain/deployment), created_at (text default CURRENT_TIMESTAMP)
- `memes`: id (integer PK auto), url (text unique), source (text), image_path (text nullable), perceptual_hash (text nullable), verified (integer default 0), confidence_score (real nullable), deployed (integer default 0), scraped_at (text default CURRENT_TIMESTAMP)
- `domains`: id (integer PK auto), name (text unique), registrar (text nullable), status (text default 'pending'), acquisition_cost_usd (real nullable), renewal_cost_usd (real nullable), renewal_date (text nullable), dns_configured (integer default 0), acquired_at (text nullable), created_at (text default CURRENT_TIMESTAMP)
- `deployments`: id (integer PK auto), site_id (integer nullable), domain_id (integer nullable), github_repo (text nullable), vercel_url (text nullable), vercel_project_id (text nullable), status (text default 'pending'), error_log (text nullable), deployed_at (text nullable), created_at (text default CURRENT_TIMESTAMP)
- `sites`: id (integer PK auto), meme_id (integer), domain_id (integer), html_path (text nullable), live_url (text nullable), prev_site_id (integer nullable), next_site_id (integer nullable), status (text default 'pending'), created_at (text default CURRENT_TIMESTAMP)
- `nostr_keypairs`: id (integer PK auto), npub (text unique), nsec_encrypted (text), relay_urls (text nullable — JSON string), created_at (text default CURRENT_TIMESTAMP)

**src/db/migrations/20260303000001_initial_schema.ts** — Kysely migration:
- Use `Kysely<any>` (NOT typed DB — frozen-in-time pattern per research)
- Create all 7 tables with proper column types, constraints, NOT NULL where appropriate
- Add indexes: `idx_transactions_created_at` on transactions(created_at), `idx_transactions_category` on transactions(category), `idx_memes_url` on memes(url), `idx_domains_name` on domains(name), `idx_domains_status` on domains(status), `idx_sites_status` on sites(status)
- Implement both `up()` and `down()` functions
- Use `sql` tagged template for CURRENT_TIMESTAMP defaults

**src/db/client.ts** — Database connection:
- Import `Database` from `better-sqlite3`, `Kysely`, `SqliteDialect` from `kysely`
- Import `getDataDir` from `../config/paths.js`
- Singleton pattern: `let db: Kysely<DatabaseSchema> | null = null`
- `getDb()` function: Create db path at `{getDataDir()}/state.db`, enable WAL mode via `sqlite.pragma('journal_mode = WAL')`, return Kysely instance
- `initializeDb()` function: Run Kysely migrations using `FileMigrationProvider` pointing to `./migrations` directory. Handle ESM `import.meta.url` for migration folder path resolution (use `fileURLToPath` + `dirname`). Return migration results. Log migration names on success.
- `closeDb()` function: Destroy Kysely instance and reset singleton
- Ensure parent directory exists before creating database (use `fs.mkdirSync` with `recursive: true`)
  </action>
  <verify>
    <automated>cd /Users/georgedrag/APP_PROJECTS/vance.digital && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>DatabaseSchema type exported with all 7 table interfaces, migration creates all tables with indexes, client connects with WAL mode, migrations run without error</done>
</task>

<task type="auto">
  <name>Task 3: Create config system with XDG paths and structured logger</name>
  <files>src/config/paths.ts, src/config/schema.ts, src/config/loader.ts, src/cli/ui/logger.ts, src/cli/ui/colors.ts</files>
  <action>
**src/config/paths.ts** — XDG directory resolution:
- Import `xdgBasedir` (default import from xdg-basedir)
- Import `os` and `path` from node builtins
- `getConfigDir()`: Returns `path.join(xdgBasedir.config ?? path.join(os.homedir(), '.config'), 'domainweave')`. Always handle undefined per research Pitfall 5.
- `getDataDir()`: Returns `path.join(xdgBasedir.data ?? path.join(os.homedir(), '.local', 'share'), 'domainweave')`
- `getConfigPath()`: Returns `path.join(getConfigDir(), 'config.json')`
- `getDbPath()`: Returns `path.join(getDataDir(), 'state.db')`

**src/config/schema.ts** — Zod validation schema:
- Define `ConfigSchema` using Zod:
  ```
  budget: { monthlyCapUsd: number (positive, default 25) }
  apiKeys: { reddit: string optional, anthropic: string optional, cloudflare: string optional, github: string optional, vercel: string optional }
  scraping: { sources: string[] default ['reddit'], memesPerSource: number default 50 }
  deployment: { vercelOrgId: string optional }
  ```
- Export `type Config = z.infer<typeof ConfigSchema>`
- Export `DEFAULT_CONFIG` object with all defaults filled in (monthlyCapUsd: 25, empty apiKeys, default scraping prefs)

**src/config/loader.ts** — Config loading with env var precedence:
- `loadConfig()`: Read config.json from `getConfigPath()`. If file doesn't exist, return defaults.
- Merge env vars on top of file config (env ALWAYS overrides per user decision):
  - `DOMAINWEAVE_REDDIT_KEY` → apiKeys.reddit
  - `DOMAINWEAVE_ANTHROPIC_KEY` → apiKeys.anthropic
  - `DOMAINWEAVE_CLOUDFLARE_KEY` → apiKeys.cloudflare
  - `DOMAINWEAVE_GITHUB_KEY` → apiKeys.github
  - `DOMAINWEAVE_VERCEL_KEY` → apiKeys.vercel
  - `DOMAINWEAVE_BUDGET_CAP` → budget.monthlyCapUsd (parse as number)
- Validate merged config with `ConfigSchema.parse()`
- `writeDefaultConfig()`: Write DEFAULT_CONFIG to config path, creating directories with `recursive: true`

**src/cli/ui/colors.ts** — Semantic color scheme (Claude's discretion):
- Import `pc` from `picocolors`
- Export color functions: `success(text)` → pc.green, `warning(text)` → pc.yellow, `error(text)` → pc.red, `info(text)` → pc.cyan, `dim(text)` → pc.dim, `bold(text)` → pc.bold, `label(text)` → pc.blue
- Export `SYMBOLS` object: `check: '✓'`, `cross: '✗'`, `warning: '⚠'`, `arrow: '→'`, `bullet: '●'`

**src/cli/ui/logger.ts** — Structured logger:
- `createLogger(options: { quiet?: boolean })` factory function
- Logger methods: `info(msg)`, `success(msg)`, `warn(msg)`, `error(msg)`, `debug(msg)`, `step(msg)` (for "Creating database... done" style)
- Info/success/warn/debug go to `process.stderr` (so stdout stays clean for data per research)
- Errors always go to `process.stderr` regardless of quiet mode
- In quiet mode (`options.quiet === true` OR `!process.stdout.isTTY`): suppress info/success/debug, only show warn+error
- Include ISO timestamp prefix in each log line
- Use semantic colors from colors.ts
  </action>
  <verify>
    <automated>cd /Users/georgedrag/APP_PROJECTS/vance.digital && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Config loads from XDG paths with env var override, Zod validates and rejects invalid config, logger respects quiet mode and outputs to stderr, all modules compile without type errors</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. All files exist in their specified paths
3. `node --loader tsx src/db/client.ts` can be imported without runtime errors
4. Config paths resolve correctly on macOS (handle xdg-basedir undefined case)
</verification>

<success_criteria>
- TypeScript ESM project compiles cleanly
- All 7 database tables defined in schema and migration
- Config system resolves XDG paths with fallbacks
- Environment variables override config file values
- Logger outputs to stderr with timestamps and severity
- Module exports are clean and consumable by Plans 02 and 03
</success_criteria>

<output>
After completion, create `.planning/phases/01-state-foundation-cli-scaffold/01-01-SUMMARY.md`
</output>
